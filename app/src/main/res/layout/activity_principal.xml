<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".activity.PrincipalActivity"
    android:background="@drawable/musculacao1"
    android:backgroundTintMode="add">

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Exibir Lista de Usuários"
        android:id="@+id/btnusuario"
        android:layout_centerVertical="true"
        android:layout_alignParentEnd="true" />

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Exibir lista de Treinadores"
        android:id="@+id/btntreinador"
        android:layout_below="@+id/btnusuario"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Exibir Lista de Modalidades"
        android:id="@+id/btnModalidade"
        android:layout_below="@+id/btntreinador"
        android:layout_alignEnd="@+id/btntreinador" />

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Exibir Lista de Treinos"
        android:id="@+id/btnTreino"
        android:layout_below="@+id/btnModalidade"
        android:layout_alignParentStart="true" />
</RelativeLayout>
